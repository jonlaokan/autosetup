---
- name: 'Install core packages'
  yum:
    name: "{{ core_pkgs }}"
    state: installed

- name: 'Install python packages'
  yum:
    name: "{{ python_pkgs }}"
    state: installed

- name: 'Install java packages'
  yum:
    name: "{{ java_pkgs }}"
    state: installed
